<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="app.daazi.v5.appclientevip">

    <!--Permissões nivel normal-->
    <uses-permission android:name="android.permission.INTERNET"/> <!--Permissão para o uso da intenet-->

    <!--Permissões nivel Perigosa-->
    <uses-permission android:name="android.permission.CAMERA"/> <!--Permissão para o uso da camera-->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/> <!--Permissão para saber a localização do usuário-->
    <uses-permission android:name="android.permission.READ_CONTACTS"/> <!--Permissão para ler os contatos do usuário-->
    <uses-permission android:name="android.permission.SEND_SMS"/> <!--Permissão para o envio de SMS-->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".view.AtualizarMeusDadosActivity"/>
        <activity android:name=".view.MeusDadosActivity" />
        <activity android:name=".view.ConsultarClientesActivity" />
        <activity android:name=".view.RecuperarSenhaActivity" />
        <activity android:name=".view.ClientePessoaJuridicaActivity" />
        <activity android:name=".view.ClientePessoaFisicaActivity" />
        <activity android:name=".view.ClienteVipActivity" />
        <activity
            android:name=".view.LoginActivity"
            android:noHistory="true"
            android:theme="@style/AppTheme" />
        <activity android:name=".view.MainActivity" />
        <activity android:name=".view.CreadencialDeAcessoActivity" />
        <activity android:name=".view.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>